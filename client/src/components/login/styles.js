const { makeStyles } = require("@material-ui/core");

export const useStyles = makeStyles({
  container: { marginTop: "45px" },
  paper: {
    display: "grid",
    width: "350px",
  },
  input: {
    margin: "5px",
  },
});
